# v0.1 Benchmark Notes

Formal Criterion benchmarks are not yet meaningful because the crate is still dominated by API shape validation rather than performance tuning.

Expected overhead for the implemented outcome types is the same branch structure as equivalent hand-written `match` or `Result` code. Revisit with Criterion once APIs stabilize after external feedback.
